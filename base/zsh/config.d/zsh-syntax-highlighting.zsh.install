#!/bin/sh
if [ ! -d $HOME/opt2/zsh-syntax-highlighting ]; then
  echo "install zsh-syntax-highlighting"
  if [ ! -d $HOME/opt2 ]; then
    mkdir -p $HOME/opt2/
  fi
  git clone https://github.com/zsh-users/zsh-syntax-highlighting.git $HOME/opt2/zsh-syntax-highlighting
fi
